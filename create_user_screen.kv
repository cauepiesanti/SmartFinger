<CreateUserScreen>:
    BoxLayout:
        orientation: 'vertical'
        
        Label:
            text: 'Criar Usuário' 

        TextInput:
            id: userName_input
            hint_text: 'Usuário'
            multiline: False
            on_text: root.remove_spaces(self)
        
        TextInput:
            id: password_input
            hint_text: 'Senha'
            multiline: False
            on_text: root.remove_spaces(self)

        Button:
            text: 'Voltar'
            on_press: root.back(root)
        
        Button:
            text: 'Confirmar cadastro'
            on_press: root.returnLogin(root)
    
        Label:
            id: message
